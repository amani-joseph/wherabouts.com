---
phase: 10-advanced-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/api/src/shared/routing-queries.ts
  - packages/api/src/shared/routing-queries.test.ts
  - packages/api/src/routers/public/routing.ts
  - packages/api/src/routers/public/routing.test.ts
  - packages/api/src/routers/public-http.ts
  - infra/osrm/build-graph.sh
  - infra/osrm/Caddyfile
  - infra/osrm/SELF-HOSTING.md
requirements:
  - ROUTE-MATRIX-01
  - ROUTE-PROFILE-01

must_haves:
  truths:
    - "routing-queries.ts exposes a profile-parameterised OSRM URL builder (driving|walking|cycling → car|foot|bike) without breaking fetchOsrmRoute"
    - "fetchOsrmTable(coords, options) calls /table/v1/{profile}/{coords}?annotations=duration,distance and returns row-major durations[][] + distances[][]"
    - "directions accepts profile=driving|walking|cycling and forwards the mapped OSRM profile"
    - "GET/POST /api/v1/routing/matrix returns { durations, distances, sources, destinations } for N origins × M destinations"
    - "fetchImpl is globalThis.fetch.bind(globalThis) everywhere OSRM is called (no Illegal invocation)"
    - "every new GET numeric input uses z.coerce.number(); no bare z.number() on a GET param"
    - "infra/osrm/build-graph.sh builds car + bike + foot graphs; Caddyfile routes profiles by path"
    - "pnpm -F @wherabouts.com/api test passes including new matrix + profile cases against mocked OSRM"
  artifacts:
    - path: "packages/api/src/shared/routing-queries.ts"
      provides: "Generalised OSRM client: route + table helpers, profile mapping"
      contains: "fetchOsrmTable"
    - path: "packages/api/src/routers/public/routing.ts"
      provides: "routingMatrix procedure + profile-widened directions"
      contains: "routingMatrix"
    - path: "infra/osrm/build-graph.sh"
      provides: "Multi-profile graph build (car/bike/foot)"
      contains: "foot.lua"
  key_links:
    - from: "packages/api/src/routers/public-http.ts"
      to: "packages/api/src/routers/public/routing.ts"
      via: "routing.matrix registered in the routing: block"
      pattern: "matrix: routingMatrix"
---

<objective>
Lay the multi-profile + matrix foundation. Generalise the OSRM client from a driving-only
`fetchOsrmRoute` into a profile-aware client that also exposes `/table` (matrix), widen
`directions` to accept `walking`/`cycling`, add the `/api/v1/routing/matrix` endpoint, and
extend the OSRM infra to build and serve the foot/bike graphs.

Purpose: Every other plan in this phase (isochrone sampling, match, optimize) reuses the
profile-aware OSRM client introduced here. Matrix + profiles are the two cheapest parity wins
and exercise the generalisation end-to-end. **Resolve D1 (how profiles are served) before the
infra task.**

Output: A profile-parameterised OSRM client, a working `/matrix` endpoint, profile-aware
`directions`, multi-profile OSRM build/serve config, and green API tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-advanced-routing/CONTEXT.md
@packages/api/src/shared/routing-queries.ts
@packages/api/src/shared/routing-queries.test.ts
@packages/api/src/routers/public/routing.ts
@packages/api/src/routers/public/routing.test.ts
@packages/api/src/routers/public-http.ts
@packages/api/src/routers/public/regions.ts
@infra/osrm/build-graph.sh
@infra/osrm/Caddyfile
</context>

<tasks>

<task type="auto">

## Task 1 — Generalise the OSRM client (profile mapping + URL builder)

**Files:** `packages/api/src/shared/routing-queries.ts` (edit)

1. Add a public profile type + mapping. Public API speaks `driving|walking|cycling`; OSRM speaks
   `car|foot|bike`:
   ```
   export type RoutingProfile = "driving" | "walking" | "cycling";
   const OSRM_PROFILE: Record<RoutingProfile, string> = {
     driving: "car", walking: "foot", cycling: "bike",
   };
   ```
2. Extract the shared OSRM call plumbing into one private helper that takes the OSRM `service`
   (`route` | `table`), `profile`, `coords` string, and a query record — builds the URL against
   `options.baseUrl`, sets the `authorization: Bearer` header, and returns parsed JSON or throws
   `RoutingError("unavailable", ...)`. Reuse the existing bind-safe `fetchImpl` contract.
3. Refactor `fetchOsrmRoute` to accept a `profile: RoutingProfile` (default `"driving"`) and use
   the shared helper with `service="route"`. **Do not change its return shape or its existing
   call site's behaviour for driving** — the existing `routing-queries.test.ts` cases must still
   pass unchanged.

**Verify:** `pnpm -F @wherabouts.com/api test routing-queries` — existing route tests still green.

**Commit:** `refactor(api): make OSRM client profile-aware with a shared request helper`

</task>

<task type="auto">

## Task 2 — Add `fetchOsrmTable` (N×M matrix)

**Files:** `packages/api/src/shared/routing-queries.ts` (edit),
`packages/api/src/shared/routing-queries.test.ts` (edit)

1. Add `fetchOsrmTable(coords: LatLng[], options & { profile, sources?, destinations? })`:
   - Build the `;`-joined `{lng},{lat}` coordinate string (OSRM order is lon,lat).
   - Call `/table/v1/{osrmProfile}/{coords}?annotations=duration,distance` (+ `sources=` /
     `destinations=` index lists when a sub-selection is requested; omit ⇒ `all`).
   - On `code === "Ok"` return `{ durations: number[][], distances: number[][], sources, destinations }`
     (durations seconds, distances metres; cells may be `null` → keep as `null`).
   - On non-`Ok`/non-200 throw `RoutingError("unavailable", ...)`. There is no `no_route` for a
     table — individual unreachable cells are `null`, not an error.
2. Tests in `routing-queries.test.ts` (mirror the existing `osrmFetch(status, body)` mock helper):
   - happy path: a 2×2 request returns the expected `durations`/`distances` arrays.
   - non-200 ⇒ throws `RoutingError("unavailable")`.
   - `code !== "Ok"` ⇒ throws `RoutingError("unavailable")`.
   - verifies the requested URL contains `/table/v1/foot/` when `profile: "walking"`.

**Verify:** `pnpm -F @wherabouts.com/api test routing-queries` — all green.

**Commit:** `feat(api): add fetchOsrmTable for N×M routing matrices`

</task>

<task type="auto">

## Task 3 — Widen `directions` to multi-profile (SC #2, API side)

**Files:** `packages/api/src/routers/public/routing.ts` (edit),
`packages/api/src/routers/public/routing.test.ts` (edit)

1. Change the `directions` input `profile` from `z.literal("driving").default("driving")` to
   `z.enum(["driving", "walking", "cycling"]).default("driving")`.
2. Pass `input.profile` through to `fetchOsrmRoute`. Keep the existing error mapping
   (`no_route` → `UNPROCESSABLE_CONTENT`, other `RoutingError` → `INTERNAL_SERVER_ERROR`).
3. Tests: add a case asserting `profile=walking` reaches OSRM as the `foot` profile (mock the
   OSRM fetch, assert the URL path); assert an invalid profile (e.g. `flying`) yields a 400.

**Verify:** `pnpm -F @wherabouts.com/api test routing` — directions multi-profile cases green.

**Note:** This is *code-complete* for SC #2, but SC #2 is only **end-to-end verifiable** once the
foot/bike graphs are deployed (Task 5). Record that dependency in the SUMMARY.

**Commit:** `feat(api): accept walking|cycling profile on routing.directions`

</task>

<task type="auto">

## Task 4 — `/api/v1/routing/matrix` endpoint (SC #1)

**Files:** `packages/api/src/routers/public/routing.ts` (edit),
`packages/api/src/routers/public/routing.test.ts` (edit),
`packages/api/src/routers/public-http.ts` (edit)

1. Add `routingMatrix` procedure following the `routingDirections` pattern: `apiKeyAuth` +
   `usageMiddleware("routing.matrix")`, `route({ method, path: "/api/v1/routing/matrix",
   summary, tags: ["routing"] })`.
   - **Method per D5.** If GET: accept `sources` + `destinations` as delimited `"lat,lng|lat,lng"`
     strings and **coerce with `z.coerce`** for any numeric scalar (bare `z.number()` 400s on
     GET). If POST: accept a JSON body `{ profile?, sources: [{lat,lng}|{addressId}], destinations:[...] }`
     (POST bodies validate normally — no coercion gotcha). Default profile `driving`.
   - Resolve every point through the existing `resolveEndpoint`/`resolveAddressCoords` helpers so
     `addressId` inputs work uniformly.
   - Cap the request size (e.g. reject if `sources.length * destinations.length` exceeds a
     documented internal limit) and return `BAD_REQUEST` with a clear message.
   - Call `fetchOsrmTable`, return `{ query: { profile }, durations, distances, sources, destinations }`.
   - Map OSRM failure → `INTERNAL_SERVER_ERROR` ("Routing service unavailable.").
2. Register it in `public-http.ts`:
   ```
   routing: {
     directions: routingDirections,
     matrix: routingMatrix,
   }
   ```
   If the frontend API-explorer endpoint allowlist is GET-only and `/matrix` is POST, mark it
   docs-only there (memory: "API explorer proxy GET-only allowlist") — do not add a POST to the
   GET allowlist.
3. Tests in `routing.test.ts`: 2×2 happy path (mocked OSRM table) returns the matrix; oversize
   request → 400; OSRM failure → 500; an `addressId`-based point resolves to coords.

**Verify:** `pnpm -F @wherabouts.com/api test routing` green; `pnpm -F @wherabouts.com/api typecheck`
clean.

**Commit:** `feat(api): add /api/v1/routing/matrix N×M duration/distance endpoint`

</task>

<task type="checkpoint">

## Task 5 — Multi-profile OSRM build + serve (SC #2, infra) — D1 checkpoint

**Human checkpoint — resolve D1 (CONTEXT.md) before editing infra and confirm host sizing.**
Each served profile memory-maps its own graph (~3–4 GB RAM each for AU); serving car+bike+foot
roughly triples the serve-side footprint. The user must confirm the host can hold all three (or
choose a subset / separate deployments).

Assuming **D1=(a): one Caddy front, profile in the OSRM path**:

**Files:** `infra/osrm/build-graph.sh` (edit), `infra/osrm/Caddyfile` (edit),
`infra/osrm/SELF-HOSTING.md` (edit), `infra/osrm/DEPLOY.md` (edit), `infra/osrm/entrypoint.sh`
(edit — start three `osrm-routed` instances on distinct localhost ports).

1. `build-graph.sh`: after the car build, build bike + foot from the same `.osm.pbf` into
   distinct output names:
   - `osrm-extract -p /opt/bicycle.lua` → partition → customize (bike graph)
   - `osrm-extract -p /opt/foot.lua` → partition → customize (foot graph)
   (The stock OSRM image ships `car.lua`, `bicycle.lua`, `foot.lua` under `/opt`.)
2. `entrypoint.sh`: launch `osrm-routed` per profile on `:5001/:5002/:5003` (localhost only),
   each pointing at its graph; keep the existing "if OSRM dies, take the container down" trap for
   all three.
3. `Caddyfile`: route `/route/v1/car/* /table/v1/car/* /match/v1/car/* /trip/v1/car/*` → :5001,
   `*/bike/*` → :5002, `*/foot/*` → :5003, behind the existing bearer-token gate. The Worker keeps
   one `OSRM_BASE_URL`; only the `{profile}` path segment varies.
4. Update `SELF-HOSTING.md`/`DEPLOY.md` sizing tables to reflect three served graphs.

**Verify (infra smoke, post-deploy — record in SUMMARY, may be deferred to deploy window):**
- `osrm-check.sh`/`smoke-test.sh` extended to hit `/route/v1/foot/...` and `/route/v1/bike/...`
  and assert HTTP 200 + `code: "Ok"`.
- End-to-end: `directions` with `profile=walking` against the live OSRM returns a foot route.

**Commit:** `feat(infra): build and serve OSRM foot + bike profiles alongside car`

</task>

</tasks>

<verification>
- SC #1 met when `/api/v1/routing/matrix` returns N×M `durations`/`distances` (unit-tested vs
  mocked OSRM `/table`; spot-checked against live OSRM).
- SC #2 *code* met when `directions` accepts `walking|cycling` and forwards `foot|bike`; SC #2
  *fully* met when the foot/bike graphs are deployed and a live `profile=walking` call returns a
  route. The infra task is the gating dependency and is the only checkpoint here.
- No bare `z.number()` on any GET param; OSRM always called via bound `fetchImpl`.
</verification>
