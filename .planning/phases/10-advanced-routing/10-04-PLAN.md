---
phase: 10-advanced-routing
plan: 04
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - packages/api/src/shared/routing-queries.ts
  - packages/api/src/shared/routing-queries.test.ts
  - packages/api/src/routers/public/routing.ts
  - packages/api/src/routers/public/routing.test.ts
  - packages/api/src/routers/public-http.ts
requirements:
  - ROUTE-OPT-01

must_haves:
  truths:
    - "fetchOsrmTrip(waypoints, options) calls /trip/v1/{profile}/{coords} with roundtrip/source/destination and geometries=geojson"
    - "POST /api/v1/routing/optimize accepts >=2 waypoints (coords or addressId) and returns the optimised visiting order + trip geometry/distance/duration"
    - "the response exposes each waypoint's visiting order (waypoint_index) so callers can reorder their stops"
    - "OSRM trip NotImplemented (unsupported roundtrip/source/destination combo) and NoTrip are mapped to clear 4xx/5xx errors"
    - "the optimize call uses the profile-aware bound-fetch client from plan 01"
    - "VROOM is documented as a deferred fast-follow per D2; v1 ships OSRM Trip (TSP) only"
    - "pnpm -F @wherabouts.com/api test passes including optimize cases"
  artifacts:
    - path: "packages/api/src/shared/routing-queries.ts"
      provides: "fetchOsrmTrip helper"
      contains: "fetchOsrmTrip"
    - path: "packages/api/src/routers/public/routing.ts"
      provides: "routingOptimize procedure"
      contains: "routingOptimize"
  key_links:
    - from: "packages/api/src/routers/public-http.ts"
      to: "packages/api/src/routers/public/routing.ts"
      via: "routing.optimize registered in the routing: block"
      pattern: "optimize: routingOptimize"
---

<objective>
Add route optimisation: given a set of stops, return a near-optimal visiting order via OSRM
`/trip` (TSP, greedy farthest-insertion / brute force), with the trip geometry and per-waypoint
ordering.

Purpose: Optimisation is the last parity endpoint and, scoped to OSRM `/trip`, needs **zero new
infra** — exactly why D2 leans OSRM-Trip-for-v1 over a VROOM sidecar. **Confirm D2 before
starting**; if the user insists on true VRP now, this plan grows a VROOM sidecar (separate infra
task) instead.

Output: `/api/v1/routing/optimize` plus `fetchOsrmTrip`, with tests, and VROOM noted as deferred.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-advanced-routing/CONTEXT.md
@packages/api/src/shared/routing-queries.ts
@packages/api/src/shared/routing-queries.test.ts
@packages/api/src/routers/public/routing.ts
@packages/api/src/routers/public/routing.test.ts
@packages/api/src/routers/public-http.ts
</context>

<tasks>

<task type="checkpoint">

## Task 1 — Confirm D2 (OSRM Trip vs VROOM)

**Human checkpoint — confirm the optimisation backend before building.** Default and recommended:
**OSRM `/trip` (symmetric TSP, no time windows/capacities/multi-vehicle)** — zero new infra.
VROOM would add a sidecar service to deploy, secure, and operate and is deferred to a fast-follow.

- If D2 = OSRM Trip ⇒ proceed with Tasks 2–3 as written.
- If D2 = VROOM now ⇒ STOP and re-plan: a VROOM sidecar (Docker service + env vars
  `VROOM_BASE_URL`/auth, Caddy gate) becomes a prerequisite infra task and the endpoint posts a
  VROOM job spec instead of calling `/trip`.

Record the decision in CONTEXT.md D2 + this plan's SUMMARY.

**Commit:** none (decision) — capture in SUMMARY.

</task>

<task type="auto">

## Task 2 — `fetchOsrmTrip`

**Files:** `packages/api/src/shared/routing-queries.ts` (edit),
`packages/api/src/shared/routing-queries.test.ts` (edit)

1. Add `fetchOsrmTrip(waypoints, options & { profile, roundtrip?, source?, destination? })`:
   - Build the `;`-joined `{lng},{lat}` coordinate string.
   - Call `/trip/v1/{osrmProfile}/{coords}` with `geometries=geojson`, `overview=full`, and
     `roundtrip` (default `true`), `source` (`any|first`), `destination` (`any|last`). Note OSRM
     does not support all combinations — validate the requested combo against the supported set
     and surface a clear error if unsupported.
   - On `code === "Ok"` return `{ trips: [{ distance_m, duration_s, geometry }],
     waypoints: [{ waypoint_index, trips_index, location }] }` — `waypoint_index` is the visiting
     order, which is the whole point of the endpoint.
   - Map `code === "NotImplemented"` (bad combo) and `code === "NoTrip"` to a `RoutingError` with a
     descriptive message; non-200/other → `unavailable`.
2. Tests (extend `osrmFetch` mock): happy path returns `trips` + ordered `waypoints`; URL contains
   `/trip/v1/car/` and `roundtrip=false` when requested; `NoTrip`/`NotImplemented` and non-200
   each map to the right `RoutingError`.

**Verify:** `pnpm -F @wherabouts.com/api test routing-queries` green.

**Commit:** `feat(api): add fetchOsrmTrip for TSP route optimisation`

</task>

<task type="auto">

## Task 3 — `/api/v1/routing/optimize` endpoint (SC #5)

**Files:** `packages/api/src/routers/public/routing.ts` (edit),
`packages/api/src/routers/public/routing.test.ts` (edit),
`packages/api/src/routers/public-http.ts` (edit)

1. Add `routingOptimize`: `apiKeyAuth` + `usageMiddleware("routing.optimize")`,
   `path: "/api/v1/routing/optimize"`, `tags: ["routing"]`. **POST** per D5 (stop lists can be
   long).
   - Input (JSON body): `profile` (`driving|walking|cycling`, default driving),
     `waypoints: [{lat,lng}|{addressId}]` with `min(2)`, optional `roundtrip` (boolean, default
     true), `source` (`any|first`), `destination` (`any|last`). Resolve each waypoint via the
     existing `resolveEndpoint`/`resolveAddressCoords`. Cap the waypoint count (documented limit).
   - Call `fetchOsrmTrip`, return `{ query: { profile, roundtrip }, trips, waypoints }` where each
     input waypoint is echoed with its resolved coords + its optimised `order` (`waypoint_index`).
   - Errors: unsupported combo / `NoTrip` → `UNPROCESSABLE_CONTENT`; `unavailable` → `INTERNAL_SERVER_ERROR`.
2. Register in `public-http.ts` under `routing:` as `optimize: routingOptimize`. POST ⇒ docs-only
   in the GET-only API-explorer allowlist.
3. Tests: happy path (mock trip) returns ordered waypoints + geometry; `< 2` waypoints → 400;
   oversize → 400; `addressId` waypoint resolves; `NoTrip` → 422; OSRM down → 500.

**Verify:** `pnpm -F @wherabouts.com/api test routing` green; `typecheck` clean.

**Commit:** `feat(api): add /api/v1/routing/optimize TSP ordering endpoint`

</task>

</tasks>

<verification>
SC #5 met when `/api/v1/routing/optimize` returns a near-optimal visiting order (each waypoint's
`waypoint_index`) plus the trip geometry/distance/duration via OSRM `/trip`, handles unsupported
combinations and `NoTrip` cleanly, and uses the profile-aware bound-fetch client. VROOM is
explicitly deferred per D2. All cases unit-tested against mocked OSRM.
</verification>
