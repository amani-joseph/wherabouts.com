---
phase: 08-teams-creation-email-invitations-resend-and-auto-generated-per-member-api-keys-scoped-to-the-team
plan: 07
type: execute
wave: 7
depends_on: ["08-06"]
files_modified:
  - apps/web/src/routes/_protected/projects.tsx
  - apps/web/src/routes/_protected/api-keys.tsx
autonomous: false
requirements: []
must_haves:
  truths:
    - "projects.tsx fetches and filters projects by the active teamId"
    - "api-keys.tsx fetches keys by active teamId and shows 'Reveal key' only on the current user's own keys"
    - "Non-owner rows on api-keys.tsx show only suffix, last-used date, usage count — no Reveal button"
    - "Switching teams via TeamSwitcher causes both routes to refetch with new teamId"
  artifacts:
    - path: "apps/web/src/routes/_protected/projects.tsx"
      provides: "Projects list scoped to active team"
      contains: "useActiveOrganization"
    - path: "apps/web/src/routes/_protected/api-keys.tsx"
      provides: "API keys list scoped to active team with owner-gated Reveal"
      contains: "revealApiKeySecret"
  key_links:
    - from: "apps/web/src/routes/_protected/projects.tsx"
      to: "apps/web/src/lib/auth-client.ts useActiveOrganization"
      via: "teamId prop threaded into server fn calls"
      pattern: "useActiveOrganization"
    - from: "apps/web/src/routes/_protected/api-keys.tsx"
      to: "apps/web/src/lib/api-keys-server.ts listApiKeys + revealApiKeySecret"
      via: "queries keyed by teamId; reveal gated by userId === session.user.id"
      pattern: "revealApiKeySecret"
---

<objective>
Thread the active team's `teamId` through the two downstream routes that previously assumed user-owned data: `/projects` and `/api-keys`. Add the owner-gated "Reveal key" button on `/api-keys` using `OneTimeKeyModal` in `mode="reveal"`.

Purpose: Complete the CONTEXT.md requirement that "projects and api-keys routes are updated to pass active teamId into server functions." This is the last step before phase UAT.
Output: Two route files updated. No schema or server-fn changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-teams-creation-email-invitations-resend-and-auto-generated-per-member-api-keys-scoped-to-the-team/08-06-SUMMARY.md
@.planning/phases/08-teams-creation-email-invitations-resend-and-auto-generated-per-member-api-keys-scoped-to-the-team/08-UI-SPEC.md

@apps/web/src/routes/_protected/projects.tsx
@apps/web/src/routes/_protected/api-keys.tsx
@apps/web/src/lib/auth-client.ts
@apps/web/src/lib/api-keys-server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread active teamId through /projects route</name>
  <files>apps/web/src/routes/_protected/projects.tsx</files>
  <read_first>
    - apps/web/src/routes/_protected/projects.tsx (current structure — identify how it currently fetches projects)
    - apps/web/src/lib/auth-client.ts (useActiveOrganization hook)
    - packages/database/src/schema/projects.ts (confirm teamId column exists and is NOT NULL after Plan 02)
    - apps/web/src/lib/teams-server.ts (if there is a projects-server.ts OR if projects server fns live elsewhere — find the existing one)
  </read_first>
  <action>
    In `apps/web/src/routes/_protected/projects.tsx`:

    1. Import `useActiveOrganization` from `@/lib/auth-client.ts` if not already imported.

    2. Inside the route component, call:
       ```ts
       const { data: activeOrg, isPending: orgPending } = useActiveOrganization();
       const teamId = activeOrg?.id;
       ```

    3. Update the existing projects query (whatever server fn it uses — likely `listProjects` in `apps/web/src/lib/projects-server.ts` or similar — identify it by reading the file) so it:
       - Accepts `{ teamId }` as input.
       - Is `enabled: !!teamId`.
       - `queryKey: ["projects", teamId]` so it refetches on team switch.

    4. If the existing server fn doesn't accept `teamId`, update it to filter by `teamId` (Zod input `z.object({ teamId: z.string().uuid() })`, query `eq(projects.teamId, teamId)`), and also apply membership check via `requireMembership` pattern from `teams-server.ts`.

    5. If activeOrg is still loading: render existing loading state. If `!activeOrg` and not loading: show `"No active team"` empty state (use existing empty state component if any).

    6. Do NOT change project-create / project-rename UI copy. Only thread teamId through.

    Tabs, double quotes, `.ts` relative imports.
  </action>
  <verify>
    <automated>grep -q "useActiveOrganization" apps/web/src/routes/_protected/projects.tsx && grep -q "teamId" apps/web/src/routes/_protected/projects.tsx && grep -q "queryKey.*teamId" apps/web/src/routes/_protected/projects.tsx && pnpm -C apps/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep "useActiveOrganization" apps/web/src/routes/_protected/projects.tsx matches
    - grep "const teamId = activeOrg" apps/web/src/routes/_protected/projects.tsx matches OR grep "teamId: activeOrg" matches
    - grep "queryKey.*teamId" apps/web/src/routes/_protected/projects.tsx matches
    - grep "enabled: !!teamId" apps/web/src/routes/_protected/projects.tsx matches OR grep "enabled: Boolean(teamId)" matches
    - `pnpm -C apps/web exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Projects route refetches on team switch; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Thread active teamId through /api-keys and add owner-gated Reveal button</name>
  <files>apps/web/src/routes/_protected/api-keys.tsx</files>
  <read_first>
    - apps/web/src/routes/_protected/api-keys.tsx (current structure — how keys are listed, whether there's already a reveal UI)
    - apps/web/src/lib/api-keys-server.ts (Plan 04 output — listApiKeys, revealApiKeySecret signatures)
    - apps/web/src/lib/auth-client.ts (useActiveOrganization, useSession)
    - apps/web/src/components/one-time-key-modal.tsx (Plan 05 output — OneTimeKeyModal props)
    - .planning/phases/08-.../08-UI-SPEC.md § "API key reveal (/team or /api-keys)"
  </read_first>
  <action>
    In `apps/web/src/routes/_protected/api-keys.tsx`:

    1. Add imports:
       ```ts
       import { useState } from "react";
       import { useActiveOrganization, useSession } from "@/lib/auth-client.ts";
       import { listApiKeys, revealApiKeySecret } from "@/lib/api-keys-server.ts";
       import { OneTimeKeyModal } from "@/components/one-time-key-modal.tsx";
       import { Button } from "@/components/ui/button.tsx";
       ```

    2. Inside the component:
       ```ts
       const { data: session } = useSession();
       const { data: activeOrg } = useActiveOrganization();
       const teamId = activeOrg?.id;
       const keys = useQuery({
         queryKey: ["api-keys", teamId],
         queryFn: () => listApiKeys({ data: { teamId: teamId! } }),
         enabled: !!teamId,
       });
       const [revealedKey, setRevealedKey] = useState<string | null>(null);

       async function handleReveal(keyId: string) {
         const res = await revealApiKeySecret({ data: { keyId } });
         setRevealedKey(res.plaintext);
       }
       ```

    3. For each key row:
       - Show: name, suffix (`••••${suffix}`), last-used date (humanize with the same `daysAgo` helper or date formatter used elsewhere), usage count if available.
       - If `key.userId === session?.user.id`: render `<Button variant="ghost" size="sm" onClick={() => handleReveal(key.id)}>Reveal key</Button>`.
       - Else: no Reveal button — metadata only.

    4. Render OneTimeKeyModal at the bottom of the component:
       ```tsx
       {revealedKey && (
         <OneTimeKeyModal
           open={!!revealedKey}
           plaintextKey={revealedKey}
           mode="reveal"
           onClose={() => setRevealedKey(null)}
         />
       )}
       ```

    5. Preserve existing create-key UI if any, but update its server-fn call to pass `teamId: teamId!, memberUserId: session.user.id` (per `createApiKey` signature from Plan 04 — noting per CONTEXT.md that members cannot self-create; only owner/admin can — UI should hide the create button for role=member; derive role from `/team` members data or add a small helper query).

    6. Do NOT change existing UI copy for keys list. Copy strings for reveal modal already inside `OneTimeKeyModal` — this task just wires it up.

    7. Tabs, double quotes, `.ts`/`.tsx` imports.
  </action>
  <verify>
    <automated>grep -q "useActiveOrganization" apps/web/src/routes/_protected/api-keys.tsx && grep -q "listApiKeys" apps/web/src/routes/_protected/api-keys.tsx && grep -q "revealApiKeySecret" apps/web/src/routes/_protected/api-keys.tsx && grep -q "OneTimeKeyModal" apps/web/src/routes/_protected/api-keys.tsx && grep -q "Reveal key" apps/web/src/routes/_protected/api-keys.tsx && grep -q "key.userId === session" apps/web/src/routes/_protected/api-keys.tsx && pnpm -C apps/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep "useActiveOrganization" apps/web/src/routes/_protected/api-keys.tsx matches
    - grep "listApiKeys" apps/web/src/routes/_protected/api-keys.tsx matches
    - grep "revealApiKeySecret" apps/web/src/routes/_protected/api-keys.tsx matches
    - grep "OneTimeKeyModal" apps/web/src/routes/_protected/api-keys.tsx matches
    - grep "mode=\"reveal\"" apps/web/src/routes/_protected/api-keys.tsx matches
    - grep "Reveal key" apps/web/src/routes/_protected/api-keys.tsx matches
    - grep "key.userId === session" apps/web/src/routes/_protected/api-keys.tsx matches OR grep "userId === session?.user.id" matches
    - grep "queryKey.*teamId" apps/web/src/routes/_protected/api-keys.tsx matches OR grep "queryKey:.*api-keys.*teamId" matches
    - `pnpm -C apps/web exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>API Keys route filters by active team; Reveal button only visible on own keys; reveal uses OneTimeKeyModal in reveal mode.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — downstream routes + full phase UAT</name>
  <what-built>
    - /projects filters by active team, refetches on team switch
    - /api-keys filters by active team, shows Reveal button only on current user's keys, uses OneTimeKeyModal in reveal mode for decrypt
    - Full phase end-to-end per CONTEXT.md § Verification
  </what-built>
  <how-to-verify>
    Start dev server (`pnpm -C apps/web dev`). Complete the full E2E flow from 08-CONTEXT.md § "Verification end-to-end":

    1. Sign up User A fresh → confirm Personal team auto-created (check `team_members` table: `psql $DATABASE_URL -c "SELECT * FROM team_members WHERE user_id = '<A-uuid>'"` returns 1 row, role='owner').
       Confirm TeamSwitcher is NOT visible in sidebar (only 1 team).

    2. User A creates team "Acme" via TeamSwitcher's "Create team…" action (requires 2+ teams — after Personal + Acme, switcher becomes visible).

    3. User A switches to Acme, visits /projects, creates project `demo`. Confirm:
       - Project appears in /projects listing.
       - `psql $DATABASE_URL -c "SELECT slug, team_id FROM projects WHERE slug='demo'"` shows team_id = Acme's id (NOT User A's Personal team id).

    4. User A visits /team (while active=Acme), clicks Invite Member, enters `joe+test@<your-resend-verified-domain>`, submits. Confirm:
       - Toast: `"Invitation sent to joe+test@..."`
       - Pending Invitations card shows new row.
       - Inspect Resend dashboard — email delivered with subject mentioning "Acme" and CTA `"Accept invitation"`.

    5. Open invite link in incognito (or copy /invite/<id> URL). Sign up as Joe. After signup:
       - Auto-redirect to /team?newKey=<id>.
       - OneTimeKeyModal appears with Joe's API key.
       - Click "Copy key" → clipboard contains key.
       - Click "I've copied it, continue" → redirects to /team.
       - Joe's active org is Acme (TeamSwitcher shows Acme).

    6. Test key works: `curl -H "Authorization: Bearer <joe-key>" https://<dev-host>/api/v1/addresses/autocomplete?q=Sydney`
       → Expect 200 with results.

    7. Back as User A on /team, click `···` next to Joe, click Remove member, confirm AlertDialog. Joe's row disappears.

    8. Re-run the curl from step 6 → Expect 401 (membership check rejects).

    9. User A re-invites Joe (same email). Joe accepts from incognito → new key auto-generated → OneTimeKeyModal shows a DIFFERENT plaintext.

    10. Joe visits /api-keys:
        - His key row shows name, suffix, last-used, usage count.
        - His row has `"Reveal key"` button.
        - Clicking reveals plaintext via OneTimeKeyModal in `mode="reveal"` with `"Done"` button.
        - Closing the modal works via Done OR Escape (reveal mode allows dismiss).
        - Other team members' keys (if any) show NO reveal button — metadata only.

    11. User A's /projects view: switch between Personal and Acme via TeamSwitcher → confirm project list updates (Personal shows previously-backfilled projects; Acme shows only `demo`).

    12. Negative: Joe (as `member` role) tries to create a new API key in UI → create button should be hidden or disabled. If not, confirm server rejects with 403.

    13. Check all copy strings across the phase against 08-UI-SPEC.md § Copywriting Contract. Report any deviation.

    If any step fails, describe the failure clearly with the step number.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- /projects and /api-keys both scope to active teamId
- TeamSwitcher hide/show rule honored across every route
- Reveal button only appears on the current user's own keys
- Full 13-step UAT passes per CONTEXT.md verification
</verification>

<success_criteria>
- Every CONTEXT.md § Verification step passes end-to-end
- Human checkpoint approved
- No mock data, no `console.log`, no `.forEach()` in modified files
- Phase 08 goal achieved: /team is functional multi-user workspace with invitations, auto-keys, membership-enforced middleware
</success_criteria>

<output>
After completion, create `.planning/phases/08-teams-creation-email-invitations-resend-and-auto-generated-per-member-api-keys-scoped-to-the-team/08-07-SUMMARY.md`
</output>
