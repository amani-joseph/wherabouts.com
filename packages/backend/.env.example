# Convex loads these for `convex dev` / deploy. Copy to `.env.local` and fill in.
# Clerk → API Keys → "Frontend API URL" (same as Convex docs: CLERK_JWT_ISSUER_DOMAIN)
# https://docs.convex.dev/auth/clerk
CLERK_JWT_ISSUER_DOMAIN=https://your-instance.clerk.accounts.dev
